import { DictionaryFilterPipe } from './dictionary-filter.pipe';

describe('DictionaryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DictionaryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
